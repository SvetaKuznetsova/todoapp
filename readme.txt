1) Прописать в /etc/hosts: 127.0.0.1 $APP_HOST static.$APP_HOST ($APP_HOST - тот который прописали в .env)
2) Добавить корневой сертификат docker/nginx/ssl/CA/ca.pem в доверенные
3) Сборка контейнера
	- Сборка должна выполняться в директории docker, это важно т.к. в образе прописаны относительные пути
	- см. инструкцию в docker/Readme.md
